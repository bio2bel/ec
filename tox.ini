[tox]
envlist = py

[testenv]
commands = pytest tests {posargs}
passenv = TRAVIS CI
deps =
    pytest

[testenv:coverage-report]
deps = coverage
skip_install = true
commands =
    coverage combine
    coverage report
